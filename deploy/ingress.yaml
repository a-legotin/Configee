---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: configee-web-ingress
  namespace: configee
  annotations: 
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`configee.staging.ezlab.ru`)
      kind: Rule
      services:
        - name: configee-web
          port: 80
    - match: Host(`configee.staging.box.ezlab.ru`)
      kind: Rule
      services:
        - name: configee-web
          port: 80